name: serena-remind
description: Nudge the agent back to Serena's symbolic tools on grep/read drift
platforms: [claude]
event: PreToolUse
handler:
  type: command
  command: serena-hooks remind --client=claude-code
timeout: 15
